# Copy this file to .env.local and fill in the values. Restart dev server after editing.
# Firebase Client (required for auth/UI) — Firebase Console → Project settings → General → Your apps → Web app
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase Admin (secret — never expose client-side). Use EITHER (1) or (2):
# (1) Single JSON (paste full service account JSON as one line)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...",...}
# (2) Separate vars (Vercel-friendly; from the same JSON: project_id, client_email, private_key)
# For private_key: paste the key with literal \n for newlines in Vercel
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Required for POST /api/match when triggering matching via API (e.g. cron). Set to a long random string.
# Admin sign-in is self-serve at /admin/signup; no ADMIN_EMAILS or ADMIN_PASSWORD needed.
ADMIN_SECRET=
